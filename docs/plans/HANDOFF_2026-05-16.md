HANDOFF CONTEXT
===============

USER REQUESTS (AS-IS)
---------------------
- "用不用找明镜review一遍"
- "我发现个问题呀~web端没做用户画像注入的展示。。不知道接口是不是支持啊"
- "你理解的不对，你仔细去看plugin端注入给你的模版，那里面有去噪后的静态画像，只是没传给服务端"
- "删除旧端点，新增新端点"
- "先部署修复，再做新功能"
- "开工把"
- "pulgin你publish了吗？去看看omem迭代的skills"
- "早上好，我需要确认，记忆召回注入给你的，和web端sessions管理里的内容一致，这点可以确认吗"
- "需要验证~我需要两端严格对齐~你懂我的意思吗"
- "方案2，可以看到精炼掉的内容和实际注入的吧？如果是这样，我选2"
- "我配成5呢，每次都保留5条注入吗？"
- "你这逻辑不对呀，如果maxrecallresults控制了候选数量，那么现在候选数量应该是1呀"
- "我的核心需求是可以通过config里的参数进行控制，满足不了需求，就需要重新设计参数体系呀"
- 反馈4条核心需求：1.用户画像注入内容web端也要能看到 2.maxRecallResults配置数量是否影响实际召回注入的个数 3.方案需要找灵犀/玄机评审 4.ECS内存从20%激增到30%，确认DB层新增方法是否参与了GC

GOAL
----
重新设计 recall 参数体系，让 config 参数真正控制召回注入全链路（候选/精炼/注入各阶段数量），同时完成存储对齐、画像内容展示、GC修复三项改造。

WORK COMPLETED
--------------
- session-detail 单接口刷新优化（expand=items 批量查询替代 N+1）
- ScoreBar 0值显示"—"而非"0.0%"
- 画像注入全链路：should_recall 返回 event_id -> plugin updateProfileInjected -> PATCH /v1/recall-events/{id}/profile-injected -> lancedb update_recall_event_profile
- 删除旧 session-recalls CRUD 端点（POST/GET/DELETE /v1/session-recalls），删 client.ts/tools.ts 残留引用
- Plugin v1.11.15 npm publish + 清缓存
- 后端部署：health check OK，前端 dist 上传完成
- 调研完成：similarityThreshold 和 maxRecallResults 都生效（传透到 Pipeline），maxRecallResults 控制 MMR 截断但 fetch_limit=limit*3
- 调研完成：内存激增根因 — 5个 recall 写方法 + batch_bump_access_count 全部没调 after_mutation()，是内存 20%->30% 的根因
- 调研完成：Pipeline discarded 来源 — 两阶段搜索的 min_score 过滤 + LLM 精炼两层累计
- 存储对齐方案2 已 approved（should_recall 不再内部存储，plugin 端存储时传 injected_count）

CURRENT STATE
-------------
- Git: 已 commit 未 push（GitHub 网络问题，需开梯子）
- 后端已部署线上，health check OK
- Plugin v1.11.15 已 publish
- 前端已部署
- 内存：RSS 530MB (14.7%)，从 20% 涨到 30%
- RecallEvent(12字段) + RecallItem(9字段) 双表已建好
- 工作树干净，无未提交更改

PENDING TASKS
-------------

### P0 — 参数体系重设计（师尊最新需求）
- [ ] 分析当前参数链路：maxRecallResults -> Pipeline fetch_limit(=limit*3) -> MMR截断 -> LLM精炼 -> 最终注入
- [ ] 设计新参数体系，需要区分/控制的维度：
  - 候选搜索数量（Pipeline fetch_limit）
  - LLM精炼前候选数量（MMR截断后给LLM的数量）
  - 最终注入数量（LLM精炼后保留的数量）
  - 是否需要控制精炼策略（strict/loose）
- [ ] 找灵犀(Metis)做方案预审，再找玄机(Oracle)做架构评审
- [ ] 实施参数改动（config.rs + pipeline.rs + session_recalls.rs + plugin config.ts）

### P1 — 存储对齐改造（方案2已approved）
- [ ] should_recall handler 删除内部存储逻辑（第370-480行）
- [ ] should_recall 返回完整 discarded 详情（含 refine_reasoning）
- [ ] RecallEvent 表新增 injected_count 字段
- [ ] Plugin hooks.ts 存储时传 injected_count=newResults.length
- [ ] Plugin hooks.ts 三个 return 路径都要调存储
- [ ] 前端展示"注入N条·保留M条·精炼掉K条"

### P2 — 画像注入内容 web 端可见
- [ ] Plugin hooks.ts 把 profile content 传给后端存储
- [ ] RecallEvent 新增 profile_content 字段（或单独字段）
- [ ] 后端 PATCH 端点支持更新 profile_content
- [ ] 前端 EventCard 展示画像注入内容

### P3 — GC 修复（内存 20%->30%）
- [ ] 以下方法补调 after_mutation()：
  - create_recall_event
  - batch_create_recall_items
  - update_recall_event_profile
  - delete_recall_events_by_session
  - batch_bump_access_count
- [ ] 注意不要引起 OOM 和 versions 版本爆炸
- [ ] 部署后验证内存回落

### P4 — 评审 + 验证 + 部署
- [ ] 明镜(Momus)评审所有改动，P0问题必须修复
- [ ] cargo build --release -p omem-server --no-default-features（不要交叉编译）
- [ ] plugin tsc + 升版本号 + npm publish + rm -rf ~/.cache/opencode/packages/@mingxy/cerebro@latest
- [ ] deploy.sh 部署后端
- [ ] scp 前端 dist 到 /var/www/omem-web/
- [ ] 验证内存回落 + health check

KEY FILES
---------
- omem-server/src/retrieve/pipeline.rs — 12阶段检索，fetch_limit=limit*3, stage_min_score_filter, stage_mmr_diversity, stage_llm_refine, SearchResults{results,discarded}
- omem-server/src/api/handlers/session_recalls.rs — should_recall handler(两阶段搜索+存储, 第370-480行存储逻辑需删除), PATCH profile-injected handler
- omem-server/src/store/lancedb.rs — RecallEvent/RecallItem struct/schema/CRUD, after_mutation(GC), 5个recall写方法缺GC
- omem-server/src/config.rs — 所有 OMEM_* 环境变量配置，需新增参数
- plugins/opencode/src/hooks.ts — autoRecallHook(去重+注入+存储), 三个return路径都要调updateProfileInjected
- plugins/opencode/src/config.ts — similarityThreshold/maxRecallResults 配置读取
- plugins/opencode/src/client.ts — updateProfileInjected, patch() method, ShouldRecallResponse(含event_id)
- omem-web/src/views/sessions/session-detail.tsx — expand=items单接口, EventCard展示

IMPORTANT DECISIONS
-------------------
- 方案2已 approved：should_recall 存全量（LLM精炼结果）+ plugin端 PATCH 更新 injected_count，web端同时展示精炼数和实际注入数
- discarded 来源确认：Pipeline 两阶段搜索的 min_score 过滤 + LLM 精炼两层累计，不是只有LLM精炼
- 线上验证所有 discarded 都有 refine_reasoning，说明两阶段搜索的候选都经过了LLM精炼
- maxRecallResults=1 时 fetch_limit=3 (limit*3)，Phase2 搜 limit*2=2 条候选
- GC 不急着止血，此次更新后一起解决
- 师尊认为当前参数体系不够，需要重新设计以满足"通过config参数控制全链路"的需求

EXPLICIT CONSTRAINTS
--------------------
- "每次写完代码都要找明镜评审，需要修复她提出的所有评审意见，修复完继续评审，直至全部通过才允许上线"
- "前端不应该只调用1个接口吗，现在刷新调用4个接口"
- "修改db层的时候，注意不要引起 OOM和versions版本爆炸的回归"
- "不要交叉编译~这样太慢了，直接编译就行" — 用 cargo build --release -p omem-server --no-default-features
- Plugin 发布流程：tsc -> 升版本号 -> npm publish -> rm -rf ~/.cache/opencode/packages/@mingxy/cerebro@latest
- 前端部署：scp dist 到 /var/www/omem-web/
- deploy.sh 位于 /mnt/d/dev/github/project/omem-server-source/deploy.sh
- 服务器 root@47.93.199.242

CONTEXT FOR CONTINUATION
------------------------
- 这个项目是 Cerebro（omem-server），Rust axum 后端 + TypeScript plugins + React 前端
- 当前 session 做了大量调研，结论已记录在上方 PENDING TASKS 中
- 师尊最新需求是"参数体系重设计"，这是最高优先级，需要先做方案设计+评审
- 已有的方案文档：docs/plans/2026-05-15-recall-table-split.md（表拆分方案，已实施大部分）
- Pipeline.search 关键代码：L145 fetch_limit = limit * 3, L308 Phase2 limit: Some(max_results * 2), L1032-1041 LLM refine discarded 构建
- should_recall handler 关键代码：第370-480行存储逻辑（需删除），两阶段搜索各自 pipeline.search 的 discarded 累加到 all_discarded
- Plugin hooks.ts 关键：autoRecallHook 中 newResults 是去重后实际注入的结果集，results 是 Pipeline 返回的全量
- 所有改动完成后必须走明镜评审 -> 修复 -> 再评审 -> 全部通过 -> 部署的流程
- 内存问题不紧急但不拖太久，本次更新一起修
