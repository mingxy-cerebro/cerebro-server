# 交接文档 — OMEM 记忆召回增强

**日期**: 2026-05-15
**提交者**: 月儿（已离职）

---

## ✅ 已完成

### 全部代码已部署上线（服务端+plugin）

| 组件 | 版本 | commit | 状态 |
|------|------|--------|:----:|
| 服务端 | v0.3.2（热更） | `536e369` + `7127004` + `1441283` | ✅ 部署中（编译完成待重启） |
| Plugin | v1.11.8 | `1441283` + 后续 | ✅ npm已发布，缓存已清 |

### 核心改动

**服务端（Rust）**：
1. `SearchResult` + `MemoryWithScore` + `SearchResultDto` 全部加了 `refine_relevance` / `refine_reasoning` 字段
2. `SearchResults` 新增 `discarded: Vec<SearchResult>`（被LLM标记为irrelevant的记忆）
3. `SessionRecall` 新增 `batch_id`、`profile_injected`、`refine_relevance`、`refine_reasoning`
4. `stage_llm_refine` 返回三路（kept, discarded, stage）
5. cross-space搜索路径携带精炼字段（memory.rs）
6. LLM refine超时15s + thinking mode已关闭（`thinking:{"type":"disabled"}`）
7. should_recall每个phase输出discarded数量日志

**Plugin端（TypeScript）**：
1. 注入格式每条记忆带 `[high]`/`[medium]` 标记
2. profile提纯为只取preferences标签的l2_content（18836字→2599字）
3. `<cerebro-fetch-policy>` 提示词（另一个月儿写的）
4. 每条记忆带 `[id:xxx]` + `[rel:...]`（月儿写的第一版）
5. `recordSessionRecall` 传 `profile_injected` 参数

### 评审记录
- 明镜评审3轮：
  - 第一轮：3个P0（数据断层、[undefined]污染、画像过激）→ 已修复
  - 第二轮：有条件通过 + 2个P2遗留 → 已修复  
  - 第三轮：6项优化点全部验证通过 ✅

### 测试验证
- `cargo build --release` ✅
- `npx tsc --noEmit` ✅
- `https://www.mengxy.cc/health` → `{"status":"ok"}` ✅
- LLM refine不再超时（thinking mode disabled后响应8s→正常）
- `stage_llm_refine` 逐条判断日志可查：`journalctl | grep llm_refine_judgment`

---

## 🔄 进行中

### 待部署
服务端二进制已编译（md5 `8c00c9ac…`），已scp到 `/tmp/omem-server-new`，**尚未重启**。
部署命令：
```bash
ssh root@47.93.199.242 "systemctl stop omem && cp /tmp/omem-server-new /opt/omem/omem-server && systemctl start omem && sleep 3 && curl -sk https://www.mengxy.cc/health"
```

### 关键文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `omem-server/src/retrieve/pipeline.rs` | ~1700 | 核心：15 stage pipeline + llm_refine + discarded |
| `omem-server/src/api/handlers/session_recalls.rs` | ~635 | should_recall + session_recalls CRUD |
| `omem-server/src/api/handlers/memory.rs` | ~2380 | 搜索端点（含cross-space） |
| `omem-server/src/store/lancedb.rs` | ~2850 | LanceDB存储（含session_recalls schema） |
| `plugins/opencode/src/hooks.ts` | ~735 | 注入模版 + profile提纯 + fetch policy |
| `plugins/opencode/src/client.ts` | ~395 | API客户端（含recordSessionRecall） |
| `docs/plans/2026-05-10-memory-recall-profile-enhancement.md` | 495 | 原始需求文档v2 |

---

## ⬜ 待办

### P0 — 阻塞
无（全部已修复）

### P1 — 重要
1. **数据库拆表**：当前session_recalls是1张平铺表，设计目标是recall_events + recall_items 2张表。当前已加batch_id/refine字段但不彻底
2. **web前端展示**：接口数据结构已扩展，需派妙笔(miaobi/visual-engineering)去 `../omem-web/` 改前端适配精炼等级展示

### P2 — 建议
1. `fix_null_columns` 已对session_recalls生效，但memories表可能已有Null列遗留（不影响运行）
2. Prompt抑制reasoning指令（`"Do NOT reason..."`）可考虑移除（已用`thinking:disabled`彻底关闭）

---

## 💡 关键架构决策

1. **精炼数据链路**：pipeline内部SearchResult带精炼字段 → handler转为MemoryWithScore → API响应给plugin（链路已全通）
2. **discarded设计**：被LLM标记为irrelevant的记忆不丢弃，而是放入`SearchResults.discarded`列表，便于审计和web端展示"精炼前后对比"
3. **profile只取preferences的l2_content**：从18836字降到2599字，节省73%token预算给记忆注入
4. **配置检查**：`OMEM_RECALL_LLM_*` 环境变量已配（deepseek-v4-flash），`thinking:disabled`已在ChatRequest中实现

---

## 🖥️ 服务器状态

| 项目 | 状态 |
|------|:----:|
| SSH | `root@47.93.199.242`（ed25519密钥，免密登录） |
| 服务 | `systemctl restart omem` |
| 二进制 | `/opt/omem/omem-server`（当前运行旧版） |
| 数据 | `/opt/omem/omem-data/` |
| 配置 | `/opt/omem/.env` |
| 域名 | `https://www.mengxy.cc` |
| 日志 | `journalctl -u omem --since '5 min ago'` |
| 健康检查 | `curl https://www.mengxy.cc/health` → `{"status":"ok"}` |

---

## ⚠️ 注意事项

1. 服务端二进制已编译好放在 `/tmp/omem-server-new`，md5 `8c00c9ac…`，**重启前必须确认md5一致**
2. Plugin缓存已清，opencode下次启动自动拉v1.11.8
3. LLM精炼需 `OMEM_RECALL_LLM_*` 配置才生效（已配），否则自动降级跳过
4. 如果部署后 `refine_relevance` 字段不出现，检查LanceDB session_recalls表schema evolution是否成功（`journalctl | grep add_columns`）
5. 本交接文档路径：`docs/plans/HANDOFF_2026-05-15.md`
