services:
  cerebro-server:
    image: ghcr.io/mingxy-cerebro/cerebro-server:latest
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
